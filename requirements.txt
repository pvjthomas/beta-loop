# β-Loop — Python dependencies
# Install:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   bash scripts/install-paperclip.sh

# Agent
google-adk

# Literature search
# - Paperclip (optional, quota-limited map): bash scripts/install-paperclip.sh
# - K-Dense scientific-agent-skills (Cursor reference): bash scripts/install-scientific-skills.sh
# - Open repositories (Europe PMC, PubMed, ChEMBL, Semantic Scholar, OpenAlex): stdlib urllib in agent/tools/literature_repositories.py
# gxl-paperclip==0.7.11 — not on PyPI; use scripts/install-paperclip.sh

# Analysis
numpy
pandas
scipy
wellmap
pypdf

# Cheminformatics (compound tagging, fingerprints)
rdkit

# Optional: docking pipeline (install separately if using GNINA binary)
# gnina — not on PyPI; see REQUIREMENTS.md
